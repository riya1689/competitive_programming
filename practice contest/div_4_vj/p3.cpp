#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);  
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        int a[105],b[105];
        for(int i=0; i<n ; i++)
        {
            cin >> a[i];
        }
        for(int i=0;i<n;i++)
        {
            cin >> b[i];
        }
        int h_dy = 0;
        for(int i=0;i<n;i++)
        [
            if((b[i]<=2 *a[i] && a[i] <= b[i] {
                 h_dy++;
            } 
            else(cout)
        ]
    }
   return 0;
}