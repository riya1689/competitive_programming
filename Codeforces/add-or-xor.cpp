#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);  
    cin.tie(NULL);
    long long t,a,b,x,y;
    cin >> t;
    while(t--){
        cin >> a >> b >> x >>y;
        if(a>b)
    }
   return 0;
}